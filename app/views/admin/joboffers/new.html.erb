<%= content_for :title, "ADMIN"%>

<%= render 'shared/h1_title', title:"New Job Offer" %>

<div class="container-fluid my-5">
  <div class="col-10 col-md-6 mx-auto">
    <%= render "form", joboffer: @joboffer %>
  </div>
</div>
